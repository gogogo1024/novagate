package codec

// This file is intentionally kept minimal.
// Compression/decompression is handled by helpers in protocol
// (EncodeFrameBody / DecodeFrameBody), so we don't duplicate gzip logic here.
